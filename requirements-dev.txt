# Developer tools — not needed to run the Streamlit app.
# Install with: pip install -r requirements-dev.txt
#
# To update pinned versions after upgrading locally:
#   pip freeze | grep -P 'ruff|mkdocs-material' > requirements-dev.txt
mkdocs-material==9.7.2
mkdocs-material-extensions==1.3.1
ruff>=0.9.0
