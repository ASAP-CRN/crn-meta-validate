#conda env create -f app_12.yml 

name: sl12
channels:
- conda-forge
dependencies:
- python=3.12
- pip
- pip:
  - pandas
  - streamlit

# pip freeze > requirements.txt

  # For better performance, install the Watchdog module:

  # $ xcode-select --install
  # $ pip install watchdog